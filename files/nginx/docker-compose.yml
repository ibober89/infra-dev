services:
  nginx:
    image: nginx:1.27-alpine
    container_name: velveta-nginx
    restart: unless-stopped
    environment:
      TZ: Europe/Istanbul
    ports:
      - "80:80"
      - "8080:8080"
      - "8081:8081"
      - "8082:8082"
      - "8083:8083"
      - "127.0.0.1:8084:8084"
    volumes:
      - /opt/velveta/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /opt/velveta/nginx/conf.d:/etc/nginx/conf.d:ro
      - /opt/velveta/nginx/sites-enabled:/etc/nginx/sites-enabled:ro
      - /opt/velveta/logs/nginx:/var/log/nginx
      - /etc/localtime:/etc/localtime:ro
    networks:
      - default
      - dev-frappe
      - dev-nextjs

networks:
  default:
  dev-frappe:
    external: true
    name: dev-frappe_default
  dev-nextjs:
    external: true
    name: dev-nextjs_default
